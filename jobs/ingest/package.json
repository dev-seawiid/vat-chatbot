{
  "name": "@vat/ingest",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "sync": "uv sync",
    "extract": "uv run python scripts/extract_pdfs.py",
    "parse": "uv run python scripts/parse_extracted.py",
    "chunk": "uv run python scripts/chunk_pdfs.py",
    "embed": "uv run python scripts/embed_chunks.py",
    "load": "uv run python scripts/load_to_db.py --reset"
  }
}

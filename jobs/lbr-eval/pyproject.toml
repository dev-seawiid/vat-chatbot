[project]
name = "lbr-eval"
version = "0.1.0"
description = "LegalBench-RAG retrieval eval (Pipitone 2024) — P@k/R@k for VAT chatbot"
requires-python = ">=3.12"
dependencies = [
    "langfuse>=3.0,<4.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/lbr_eval"]
